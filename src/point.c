#include <stdio.h>
#include <stdlib.h>

#include "point.h"

Tpoint* point_malloc(double x, double y, double z){

	double* tmp;
	tmp = malloc (sizeof(double));

	tmp->x;
	tmp->y;
	tmp->z;
return tmp;
}

void point_free(Tpoint*){


	free(Tpoint*);


}
